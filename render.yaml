services:
  - type: web
    name: vendaboost-login
    env: node
    plan: free
    buildCommand: cd login-system && npm install
    startCommand: cd login-system && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000