services:
  - type: web
    name: vendaboost-login
    env: node
    plan: free
    buildCommand: cd login-system && npm install
    startCommand: node app.cjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000